import React from 'react';
import {
  SafeAreaView,
  StyleSheet,
  ScrollView,
  TextInput,
  Image,
  View,
  Text,
  StatusBar,
  TouchableOpacity,
} from 'react-native';

const Feed = props => {
  return <Text>FEED</Text>;
};

const styles = StyleSheet.create({});

export default Feed;
